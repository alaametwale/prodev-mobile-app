import { View, Text } from "react-native";
import { mainStyles } from "../../styles/_mainstyle";

export default function Saved() {
  return (
    <View style={mainStyles.container}>
      <Text style={mainStyles.title}>Saved</Text>
    </View>
  );
}
